# 智能家居 LVGL UI 开发规范

> 目标平台：openvela QEMU goldfish-arm64 模拟器（466×466 圆形屏）
> 图形框架：LVGL 8.x + UIKit 字体管理 + FreeType (MiSans CJK)
> 配置宏：`CONFIG_SMART_HOME_DEMO_UI_LVGL`

---

## 目录

1. [导航设计规范](#1-导航设计规范)
2. [设备卡片组件规范](#2-设备卡片组件规范)
3. [工具调用卡片设计规范](#3-工具调用卡片设计规范)
4. [面板设备管理功能规范](#4-面板设备管理功能规范)
5. [通用设计要求](#5-通用设计要求)
6. [设置页面](#6-设置页面)
7. [交付物](#7-交付物)
8. [附录：技术约束与参考](#8-附录技术约束与参考)

---

## 1. 导航设计规范

### 1.0 视觉定位

smart_home 的 LVGL UI 应保持**明亮、高级、产品化**的智能家居中控风格。
背景不建议使用大面积深色。深色界面容易让 demo 呈现工程调试感，也会削弱
家庭设备管理场景应有的清洁、温和和可亲近感。

设计关键词：

| 关键词 | 说明 |
|--------|------|
| Bright | 主背景使用浅色暖白或浅灰，不使用纯黑、深蓝或大面积暗色 |
| Premium | 低饱和配色、克制阴影、清晰层级，避免彩色堆叠 |
| Calm | 智能家居场景应让用户感到稳定、可靠、安静 |
| Agent-aware | UI 要清楚表达 Agent 状态、工具调用和设备状态变化 |
| Embedded | 控件少而准，避免网页式复杂布局和过重装饰 |

推荐整体观感：

```text
浅色背景 + 白色/雾白卡片 + 蓝绿色状态色 + 低对比细边框
```

不推荐：

- 大面积深蓝、黑色、紫蓝渐变背景
- 依赖高饱和霓虹色表现智能感
- 用大量装饰性阴影、发光或 emoji 取代清晰信息结构
- 把 Chat 设计成主屏，弱化设备状态

信息优先级：

```text
Panel    = 主屏，设备状态与快速控制
Chat     = Agent 对话与工具调用链
Settings = 模型 API、运行状态、资源配置
```

### 1.1 导航架构

采用**底部按钮式屏幕切换导航系统**，固定于屏幕底部安全区域内。导航栏包含 3 个核心功能入口：

| 编号 | 图标 | 文字标签 | 功能 | LVGL 屏幕 |
|------|------|---------|------|-----------|
| 1 | 🏠 | 面板 | 设备管理与控制（主屏） | `screen_panel` |
| 2 | 💬 | 对话 | 聊天 + 工具调用链 | `screen_chat` |
| 3 | ⚙ | 设置 | 模型 API、网络和系统配置 | `screen_settings` |

### 1.2 导航栏布局

```
┌─────────────────────────────────────────┐
│                                         │
│           内容区域                       │
│         (可滚动)                        │
│                                         │
│                                         │
├─────────────────────────────────────────┤
│  ┌──────────┐ ┌──────────┐ ┌──────────┐│
│  │    🏠     │ │    💬     │ │    ⚙     ││
│  │   面板    │ │   对话    │ │   设置    ││
│  └──────────┘ └──────────┘ └──────────┘│
│              底部导航栏                   │
└─────────────────────────────────────────┘
```

**尺寸规范**：

| 属性 | 值 | 说明 |
|------|-----|------|
| 导航栏高度 | 56px | 包含图标 + 文字 + 上下 padding |
| 导航栏宽度 | 屏幕宽度 - 2×侧边 padding | 适配圆形屏裁切 |
| 图标尺寸 | 24×24px | 使用 LVGL Symbol 或 Unicode 字符 |
| 文字大小 | 12px | `lv_font_montserrat_12` |
| 按钮间距 | 8px | 均匀分布 |
| 导航栏底边距 | 20px | 避开圆形屏底部弧线裁切 |

### 1.3 激活状态视觉区分

| 状态 | 图标颜色 | 文字颜色 | 背景 |
|------|---------|---------|------|
| 激活 | `#4A90D9` (品牌蓝) | `#4A90D9` | 半透明蓝底 `#4A90D9@20%` |
| 非激活 | `#888888` (灰色) | `#888888` | 透明 |
| 按下 | `#3A7BD5` (深蓝) | `#3A7BD5` | 半透明蓝底 `#3A7BD5@30%` |

### 1.4 切换动画

- **动画类型**：LVGL `lv_scr_load_anim`
- **动画效果**：`LV_SCR_LOAD_ANIM_MOVE_LEFT` / `LV_SCR_LOAD_ANIM_MOVE_RIGHT`
- **动画时长**：250ms
- **方向规则**：目标屏幕编号 > 当前屏幕 → 向左滑入；反之向右滑入

```c
/* 示例代码 */
lv_scr_load_anim(target_screen,
    (target_idx > current_idx) ? LV_SCR_LOAD_ANIM_MOVE_LEFT
                               : LV_SCR_LOAD_ANIM_MOVE_RIGHT,
    250, 0, false);
```

### 1.5 导航栏实现要点

- 导航栏在每个屏幕上**独立创建**（LVGL 屏幕切换时旧屏幕的控件不可见）
- 使用 `lv_obj_align(nav_bar, LV_ALIGN_BOTTOM_MID, 0, -20)` 定位
- 按钮使用 `lv_btn` + `lv_label`，事件回调中调用 `lv_scr_load_anim`
- 当前激活按钮的 `LV_STATE_CHECKED` 通过 `lv_obj_add_state(btn, LV_STATE_CHECKED)` 设置

---

## 2. 设备卡片组件规范

### 2.1 卡片网格布局

设备控制面板采用 **2 列自适应网格** 布局：

面板页是产品主屏，不是聊天入口页。推荐结构为：

```text
Smart Home                         Ready

Living Room
  [ Light       ON       65% ]

Bedroom
  [ Light       OFF          ]
  [ AC          ON       25C ]

Temp 25C       Hum 62%       Light 500lx

Panel              Chat              Settings
```

设计要点：

- 设备状态优先于对话入口，用户打开应用首先看到家居状态。
- 设备多时按房间分组；设备少时可保持 2 列卡片，但仍应显示房间语义。
- 顶部保留 Agent 状态短文本：`Ready` / `Thinking` / `Tool running` / `Network error`。
- 卡片表达设备状态，不承载长说明；复杂操作进入控制弹窗。
- 浅色背景下通过边框、状态点、轻阴影建立层级，不使用深色块制造压迫感。

```
┌─────────────────────────────────────┐
│ Smart Home                    Ready │  ← 标题栏 (h=40px)
├─────────────────────────────────────┤
│ Living Room                         │
│  ┌───────────────┐                  │
│  │ LIGHT         │                  │
│  │ Living Light  │                  │
│  │ ON  Level 65% │                  │
│  └───────────────┘                  │
│ Bedroom                             │
│  ┌────────┐ ┌────────┐              │
│  │ LIGHT  │ │ AC     │              │
│  │ OFF    │ │ ON 25C │              │
│  └────────┘ └────────┘              │
├─────────────────────────────────────┤
│ Temp 25C   Hum 62%   Light 500lx   │  ← 传感器条 (h=32px)
├─────────────────────────────────────┤
│ [Panel]       [Chat]       [Setting]│  ← 导航栏
└─────────────────────────────────────┘
```

**卡片尺寸规范**：

| 属性 | 值 | 说明 |
|------|-----|------|
| 卡片宽度 | `(屏幕宽 - 3×间距) / 2` ≈ 196px | 2 列等宽 |
| 卡片最小高度 | 100px | |
| 卡片圆角 | 12px | `lv_obj_set_style_radius(card, 12, 0)` |
| 卡片间距 | 10px | 水平 + 垂直 |
| 卡片内边距 | 12px | 四边 |
| 网格容器上边距 | 50px | 标题栏下方 |
| 网格容器下边距 | 88px | 传感器条 + 导航栏 |

### 2.2 卡片视觉规范

#### 开启状态 (ON)

```
┌──────────────┐
│  💡           │  ← 设备图标 (24px, 白色)
│  客厅灯       │  ← 设备名称 (14px, 白色)
│  ● ON        │  ← 状态指示 (12px, 绿色 #4CD964)
│  亮度: 80%   │  ← 关键参数 (12px, 浅灰 #AAAAAA)
└──────────────┘
  背景: #E7F4F1  边框: 1px #B8DED5  阴影: 轻阴影 #182033@10%
```

#### 关闭状态 (OFF)

```
┌──────────────┐
│  💡           │  ← 设备图标 (24px, 灰色 #888)
│  客厅灯       │  ← 设备名称 (14px, 灰色 #CCC)
│  ○ OFF       │  ← 状态指示 (12px, 灰色 #888)
│              │  ← 参数不显示
└──────────────┘
  背景: #FFFFFF  边框: 1px #DCE3EC  阴影: 无或极轻阴影
```

#### 加载中状态

```
┌──────────────┐
│  💡           │
│  客厅灯       │
│  ◌ ...       │  ← 旋转动画指示器
│              │
└──────────────┘
  背景: #EEF3F8  边框: 1px #DCE3EC
```

### 2.3 设备类型图标映射

| 设备类型 | 图标字符 | Unicode | ON 颜色 | 参数 |
|---------|---------|---------|---------|------|
| light (灯) | 💡 | `\xF0\x9F\x92\xA1` | `#FFD700` 金色 | 亮度 (0-100%) |
| ac (空调) | ❄️ | `\xE2\x9D\x84` | `#4CD964` 绿色 | 温度 (16-30°C) |
| fan (风扇) | 🌀 | `\xF0\x9F\x8C\x80` | `#5AC8FA` 蓝色 | 档位 (1-3) |

> **注意**：Emoji 图标依赖 MiSans 字体支持。若 MiSans 不包含对应 Emoji，改用 LVGL 内置 Symbol 或纯文字替代方案：
> - light → `LV_SYMBOL_IMAGE` 或 "灯"
> - ac → `LV_SYMBOL_LOOP` 或 "空调"
> - fan → `LV_SYMBOL_REFRESH` 或 "风扇"

### 2.4 设备控制弹窗

点击设备卡片后弹出全屏控制界面（overlay），支持参数调节：

```
┌─────────────────────────────────────┐
│  ← 返回          客厅灯             │  ← 顶栏 (h=44px)
├─────────────────────────────────────┤
│                                     │
│           💡                        │  ← 大图标 (48px)
│                                     │
│        ┌─── ON ───┐                │  ← 开关按钮 (toggle)
│        │   ●      │                │
│        └──────────┘                │
│                                     │
│  亮度                               │  ← 参数标签
│  ○────────────────●───── 80%       │  ← 滑块 (lv_slider)
│                                     │
│  ┌──────────┐  ┌──────────┐        │
│  │   取消    │  │   确认    │        │  ← 操作按钮
│  └──────────┘  └──────────┘        │
│                                     │
└─────────────────────────────────────┘
```

**控制弹窗组件规范**：

| 组件 | LVGL 控件 | 规范 |
|------|----------|------|
| 返回按钮 | `lv_btn` + `lv_label` | 左上角，文字 "← 返回"，点击关闭弹窗 |
| 开关按钮 | `lv_btn` (toggle 模式) | 居中，尺寸 80×36px，ON=蓝色，OFF=灰色 |
| 参数滑块 | `lv_slider` | 宽度 80%，居中，范围根据设备类型设定 |
| 确认按钮 | `lv_btn` | 蓝色背景 `#4A90D9`，白色文字 |
| 取消按钮 | `lv_btn` | 灰色背景 `#555`，白色文字 |

**各设备类型参数范围**：

| 设备类型 | 参数名 | 范围 | 步进 | 默认值 |
|---------|--------|------|------|--------|
| light | 亮度 | 0-100% | 5% | 80% |
| ac | 温度 | 16-30°C | 1°C | 26°C |
| fan | 档位 | 1-3 | 1 | 2 |

### 2.5 卡片交互流程

```
用户点击卡片
    │
    ▼
显示控制弹窗 (overlay)
    │
    ├─ 拖动滑块 → 实时更新参数显示值（不提交）
    ├─ 点击开关 → 切换 ON/OFF 状态（不提交）
    │
    ├─ 点击「确认」
    │   ├─ 显示加载状态（卡片 + 弹窗同时）
    │   ├─ 调用 device_state_set() 更新内存状态
    │   ├─ 调用 lvgl_ui_device_update() 刷新卡片
    │   ├─ 关闭弹窗
    │   └─ 显示成功/失败 toast 提示
    │
    ├─ 点击「取消」
    │   └─ 关闭弹窗，不修改任何状态
    │
    └─ 点击「返回」
        └─ 关闭弹窗，不修改任何状态
```

### 2.6 状态更新实时反馈

- 设备状态变更后，卡片背景色、图标、状态文字在 **100ms 内** 完成过渡
- 使用 `lv_obj_set_style_bg_color` + `LV_ANIM_ON` 实现颜色渐变
- Agent 通过工具调用变更设备时，同样触发 UI 刷新（通过 `lvgl_ui_device_update` 回调）

---

### 2.7 房间过滤

当设备数量较多时（如 8 个房间 × 3 种设备 = 24 个），面板卡片网格会变得拥挤。提供**按房间过滤**的下拉选择器：

```
┌─────────────────────────────────────┐
│           🏠 智能家居           [▼ 全部房间]│  ← 标题栏 + 房间选择器
├─────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐        │
│  │  💡       │  │  ❄️       │        │
│  │  客厅灯   │  │  客厅空调  │        │  ← 仅显示"客厅"的设备
│  │  ● ON    │  │  ○ OFF   │        │
│  └──────────┘  └──────────┘        │
```

**房间选择器规范**：

| 属性 | 值 | 说明 |
|------|-----|------|
| 位置 | 标题栏右侧 | `lv_obj_align(dropdown, LV_ALIGN_RIGHT_MID, 0, 0)` |
| 宽度 | 100px | 显示 "全部房间 ▼" |
| 默认值 | "全部房间" | 显示所有设备 |
| 下拉项 | 全部房间 + 已存在的房间列表 | 动态生成（从 `g_devices` 遍历房间名） |
| LVGL 控件 | `lv_dropdown` | `lv_dropdown_set_options(dd, "全部房间\n客厅\n卧室\n书房")` |

**过滤规则**：

| 规则 | 说明 |
|------|------|
| 选择"全部房间" | 网格显示所有已注册设备，2 列等宽 |
| 选择特定房间 | 仅显示该房间的设备，同时显示该房间的传感器数据 |
| 房间不存在设备 | 下拉项中不显示空的房间（从 `g_devices` 动态收集） |
| 过滤切换 | 立即刷新网格，卡片重新排列，无动画（避免所有卡片同时动画的抖动） |

**实现要点**：

```c
/* 房间过滤回调 */
static void on_room_filter_changed(lv_event_t *e) {
    lv_obj_t *dd = lv_event_get_target(e);
    char selected[32];
    lv_dropdown_get_selected_str(dd, selected, sizeof(selected));

    /* 重建卡片网格 */
    rebuild_device_grid(strcmp(selected, "全部房间") == 0 ? NULL : selected);
}

/* 从 g_devices 动态收集房间列表 */
static void rebuild_room_filter_options(lv_obj_t *dd) {
    char options[256] = "全部房间";
    /* 遍历 g_devices，收集不重复的房间名 */
    for (int i = 0; i < g_device_count; i++) {
        if (room_not_in_options(options, g_devices[i].room)) {
            strlcat(options, "\n", sizeof(options));
            strlcat(options, room_display_name(g_devices[i].room), sizeof(options));
        }
    }
    lv_dropdown_set_options(dd, options);
}
```

---

## 3. 工具调用卡片设计规范

### 3.1 嵌入位置

工具调用卡片**无缝嵌入聊天信息流**中，位于用户消息气泡与 Agent 回复气泡之间：

```
  ┌────────────────────────┐
  │  User: Turn on light     │  ← 用户气泡 (右对齐, 浅蓝底)
  └────────────────────────┘
  ┌────────────────────────┐
  │  🔧 control_device     │  ← 工具调用卡片 (左对齐, 橙色边框)
  │  ├ device: light       │
  │  ├ room: living_room   │
  │  └ result: ✅ ok       │
  └────────────────────────┘
  ┌────────────────────────┐
  │  Assistant: Done         │  ← Agent 气泡 (左对齐, 白色卡片)
  └────────────────────────┘
```

### 3.2 卡片视觉规范

工具调用卡片与聊天气泡的**视觉区分**：

| 属性 | 聊天气泡 | 工具调用卡片 |
|------|---------|-------------|
| 对齐 | 右(用户) / 左(Agent) | 左对齐 |
| 背景色 | `#E8F1FE`(用户) / `#FFFFFF`(Agent) | `#FFF8EA` 浅琥珀 |
| 边框 | 1px `#DCE3EC` | 左侧 3px `#D98A14` |
| 圆角 | 14px | 8px |
| 宽度 | 85% | 80% |
| 图标 | 无 | 🔧 工具图标 |

### 3.3 卡片内容结构

```
┌────────────────────────────────────┐
│ 🔧 control_device          ✅ 成功 │  ← 标题行：工具名 + 状态
├────────────────────────────────────┤
│  device: light                     │  ← 参数区
│  room: living_room                 │
│  action: on                        │
├────────────────────────────────────┤
│  result: {"ok":true,"state":"on"}  │  ← 结果区（可折叠）
│                          [详情 ▼]  │  ← 展开/折叠按钮
└────────────────────────────────────┘
```

**卡片高度自适应规则**：

| 状态 | 高度 | 说明 |
|------|------|------|
| 加载中 | 48px | 仅显示标题行 + 旋转指示器 |
| 成功（折叠） | 72px | 标题行 + 1行参数摘要 |
| 成功（展开） | 自适应，最大 60% 屏幕高 | 标题行 + 全部参数 + 结果 |
| 失败 | 72px | 标题行 + 错误信息 + 重试按钮 |

### 3.4 调用状态指示

| 状态 | 图标 | 颜色 | 说明 |
|------|------|------|------|
| 加载中 | ◌ (旋转) | `#FF9500` 橙色 | 工具正在执行 |
| 成功 | ✅ | `#4CD964` 绿色 | 执行成功 |
| 失败 | ❌ | `#FF3B30` 红色 | 执行失败 |

**加载动画**：使用 `lv_spinner` 控件，尺寸 16×16px，旋转周期 800ms。

### 3.5 操作按钮

| 按钮 | 显示条件 | 功能 |
|------|---------|------|
| 重试 | 失败状态 | 重新执行该工具调用 |
| 详情 ▼/▲ | 成功状态 | 展开/折叠结果 JSON |
| 复制 | 展开状态 | 将结果复制到剪贴板（可选） |

### 3.6 工具调用卡片数据流

```
agent_loop.c: tool_exec_thread()
    │
    ├─ 工具执行前:
    │   smart_home_lvgl_event_cb(AGENT_EVENT_TOOL_CALL)
    │   → 在聊天流中插入「加载中」卡片
    │
    ├─ 工具执行后:
    │   smart_home_lvgl_event_cb(AGENT_EVENT_TOOL_RESULT)
    │   → 插入「成功」或「失败」工具状态卡片
    │
    └─ Agent 回复:
        smart_home_lvgl_append_msg_bubble(text)
        → 在卡片后插入 Agent 回复气泡
```

---

### 3.7 语音输入 (PTT) 交互

对话界面底部提供**按住说话 (Push-to-Talk)** 入口，与文本输入框共存：

```
┌─────────────────────────────────────────┐
│  ... 对话气泡 + 工具卡片区域 (可滚动) ...  │
├─────────────────────────────────────────┤
│  ┌──────────────────────────────┐ [🎤]  │  ← 输入框 + PTT 按钮
│  │  输入消息...                   │       │
│  └──────────────────────────────┘       │
└─────────────────────────────────────────┘
```

**PTT 按钮规范**：

| 属性 | 值 | 说明 |
|------|-----|------|
| 位置 | 输入框右侧 | 紧跟文本输入框 |
| 尺寸 | 44×44px | 触控友好 |
| 图标 | 麦克风 PNG / `LV_SYMBOL_AUDIO` | 图标缺失时显示文字 "Voice" |
| 背景 | 空闲: `#E8F1FE` / 录音中: `#FFE8E6` | 浅色底 + 红色状态点提示录音 |
| 圆角 | 22px (圆形) | 圆形按钮 |

**PTT 交互状态机**：

```
空闲 (🎤 灰色圆形按钮)
  │
  │ 用户按下 (touch down)
  ▼
录音中 (🔴 红色圆形按钮 + 脉冲动画)
  │
  ├─ 用户松开 (touch up) → 停止录音 → 发送 ASR 识别
  │     │
  │     ├─ ASR 成功 → 文本填入输入框 → 自动发送
  │     └─ ASR 失败 → 红色 toast "语音识别失败，请重试" → 回空闲
  │
  ├─ 录音超时 (60s) → 自动停止 → 发送 ASR
  │
  └─ 用户上滑 (swipe up) → 取消录音 → 回空闲
```

**录音状态视觉反馈**：

| 元素 | 空闲 | 录音中 |
|------|------|--------|
| PTT 按钮背景 | `#E8F1FE` | `#FFE8E6` + 红色状态点 + 轻脉冲 |
| 提示文字 | "按住说话" | 显示实时录音时长 "00:12" |
| 波形指示器 | 无 | 音量条（根据 PCM 幅度跳动的竖条） |

**PTT 按钮触摸事件**：

```c
/* PTT 按钮事件处理 */
static lv_obj_t *ptt_btn;

static void ptt_pressed_cb(lv_event_t *e) {
    /* 开始录音 */
    audio_capture_start();
    lv_obj_set_style_bg_color(ptt_btn, lv_palette_main(LV_PALETTE_RED), 0);
    lv_label_set_text(ptt_btn_label, LV_SYMBOL_AUDIO);  /* 或 "🔴" */
    show_recording_wave();   /* 显示波形指示器 */
}

static void ptt_released_cb(lv_event_t *e) {
    /* 停止录音，发送 ASR */
    audio_capture_stop();
    lv_obj_set_style_bg_color(ptt_btn, lv_color_hex(0x3A3A55), 0);
    lv_label_set_text(ptt_btn_label, "🎤");
    hide_recording_wave();

    /* 将 PCM 数据发送给 ASR 服务 */
    asr_submit(audio_buffer, audio_len, on_asr_result, NULL);
}
```

**文本输入回退**：PTT 是输入增强，不是替代。当用户不想说话（安静环境、隐私考虑）时，文本输入框始终可用。交互规则：

| 场景 | 行为 |
|------|------|
| PTT 识别文本返回 | 自动填入输入框 → 立即发送消息 |
| 用户正在文本输入 | 切换回文本模式，PTT 仅在按下时激活 |
| 录音中用户开始打字 | 自动取消录音 → 切换到文本输入 |
| 无音频设备 | PTT 按钮隐藏，文本输入占满整行 |

**线程安全**：音频采集在独立线程中运行（`audio_capture` 模块），ASR 回调通过 `lv_async_call` 投递到 LVGL 线程更新 UI。不可在 ASR 回调中直接操作 LVGL 控件。

---

## 4. 面板设备管理功能规范

设备管理归属于**面板**，不再作为独立导航 tab。面板既展示设备状态，也承载设备创建、编辑、删除和快速控制；设置页只负责模型 API、网络、显示和系统配置。

### 4.1 面板管理布局

```
┌─────────────────────────────────────┐
│           🏠 智能家居       ＋ 新建  │  ← 面板标题栏
├─────────────────────────────────────┤
│                                     │
│  ┌──────────────────────────────┐   │
│  │  💡 客厅灯          [编辑][删] │   │  ← 设备列表项
│  ├──────────────────────────────┤   │
│  │  💡 卧室灯          [编辑][删] │   │
│  ├──────────────────────────────┤   │
│  │  ❄️ 空调            [编辑][删] │   │
│  ├──────────────────────────────┤   │
│  │  🌀 风扇            [编辑][删] │   │
│  └──────────────────────────────┘   │
│                                     │
├─────────────────────────────────────┤
│  [🏠 面板]  [💬 对话]  [⚙ 设置]   │  ← 导航栏
└─────────────────────────────────────┘
```

### 4.2 设备创建流程

点击「＋ 新建」按钮后进入设备创建界面：

```
┌─────────────────────────────────────┐
│  ← 取消       新建设备              │
├─────────────────────────────────────┤
│                                     │
│  设备类型                            │
│  ┌──────┐ ┌──────┐ ┌──────┐       │
│  │ 💡灯  │ │ ❄️空调 │ │ 🌀风扇 │       │  ← 类型选择器
│  │ 选中  │ │      │ │      │       │
│  └──────┘ └──────┘ └──────┘       │
│                                     │
│  设备名称                            │
│  ┌──────────────────────────┐      │
│  │  客厅灯                   │      │  ← 文本输入框
│  └──────────────────────────┘      │
│                                     │
│  所在房间                            │
│  ┌──────────────────────────┐      │
│  │  living_room              │ ▼   │  ← 下拉选择
│  └──────────────────────────┘      │
│                                     │
│  初始参数                            │
│  亮度: ○────────●───── 80%        │  ← 参数滑块
│                                     │
│  ┌──────────┐  ┌──────────┐        │
│  │   取消    │  │   创建    │        │
│  └──────────┘  └──────────┘        │
│                                     │
└─────────────────────────────────────┘
```

**创建流程步骤**：

| 步骤 | 操作 | 必填 | 验证规则 |
|------|------|------|---------|
| 1. 选择设备类型 | 点击类型卡片 | 是 | 必须选择一个 |
| 2. 输入设备名称 | 文本输入框 | 是 | 2-16字符，不可与同房间同类型重名 |
| 3. 选择房间 | 下拉选择 | 是 | 预置: 客厅/卧室/书房/厨房 |
| 4. 配置初始参数 | 滑块调节 | 否 | 使用设备类型默认值 |
| 5. 确认创建 | 点击「创建」 | - | 验证通过后创建 |

**设备类型选择器规范**：

| 属性 | 值 |
|------|-----|
| 卡片尺寸 | 80×72px |
| 选中边框 | 2px `#4A90D9` |
| 未选边框 | 1px `#555` |
| 图标大小 | 24px |
| 文字大小 | 12px |

**房间下拉选项**：

| 值 | 显示名 |
|----|--------|
| living_room | 客厅 |
| bedroom | 卧室 |
| study | 书房 |
| kitchen | 厨房 |

### 4.3 设备编辑功能

点击列表项的「编辑」按钮，进入编辑界面（与创建界面类似，预填当前值）：

- 设备类型：**不可修改**（灰色禁用态）
- 设备名称：可修改
- 所在房间：可修改
- 参数：可修改

编辑确认后：
1. 调用 `device_state_set()` 更新内存
2. 调用 `lvgl_ui_device_update()` 刷新面板卡片
3. 返回面板设备列表

### 4.4 设备删除功能

点击「删除」按钮后弹出确认对话框：

```
┌──────────────────────────┐
│                          │
│  确定删除「客厅灯」吗？    │
│                          │
│  ┌────────┐ ┌────────┐  │
│  │  取消   │ │  删除   │  │
│  └────────┘ └────────┘  │
│                          │
└──────────────────────────┘
```

- 删除按钮颜色：`#FF3B30` 红色
- 确认后调用 `device_state_remove()`（需新增）
- 刷新面板卡片和设备列表

### 4.5 空状态展示

当设备列表为空时：

```
┌─────────────────────────────────────┐
│                                     │
│                                     │
│           📦                         │
│      暂无设备                        │
│   点击右上角「＋」添加设备            │
│                                     │
│                                     │
└─────────────────────────────────────┘
```

- 图标：📦 或 "空" 文字
- 提示文字：14px，灰色 `#888`
- 引导按钮：可点击跳转创建界面

### 4.6 新建设备实时显示

- 创建成功后，设备立即出现在面板卡片网格中
- 使用 `lv_async_call` 在 LVGL 线程中安全创建新卡片
- 新卡片出现时有 200ms 淡入动画（`lv_obj_set_style_opa` + `lv_anim_t`）

---

## 5. 通用设计要求

### 5.1 设计令牌 (Design Tokens)

#### 色彩系统

本项目默认主题为 **Light Premium**。深色主题不是默认目标，除非后续明确增加
主题切换并完成完整适配。所有页面应首先在浅色背景下验证可读性、层级和圆屏裁切。

| 令牌名 | 色值 | 用途 |
|--------|------|------|
| `--color-bg-page` | `#F6F8FB` | 页面主背景，明亮浅灰，不使用深色大底 |
| `--color-bg-surface` | `#FFFFFF` | 卡片、弹窗、输入区背景 |
| `--color-bg-surface-soft` | `#EEF3F8` | 次级容器、底部导航激活底 |
| `--color-bg-device-on` | `#E7F4F1` | 设备开启状态卡片背景 |
| `--color-bg-device-off` | `#FFFFFF` | 设备关闭状态卡片背景 |
| `--color-primary` | `#2F80ED` | 主操作、导航激活、链接动作 |
| `--color-primary-pressed` | `#2468C8` | 主按钮按下态 |
| `--color-accent` | `#21A67A` | 设备 ON、成功状态、健康状态 |
| `--color-warning` | `#D98A14` | 工具调用中、网络波动、非致命警告 |
| `--color-danger` | `#D64545` | 错误、删除、不可恢复操作 |
| `--color-text-primary` | `#172033` | 主文字 |
| `--color-text-secondary` | `#526070` | 辅助文字 |
| `--color-text-muted` | `#8A96A8` | 禁用、占位、低优先级提示 |
| `--color-border` | `#DCE3EC` | 卡片边框、输入框边框 |
| `--color-shadow` | `#182033@10%` | 轻阴影，仅用于浮层和重要卡片 |

色彩使用原则：

- 页面背景保持浅色，优先 `--color-bg-page`。
- 卡片默认白色，靠边框和轻阴影形成层级，不用深色块制造对比。
- ON 状态使用浅蓝绿底 + 深色文字 + `--color-accent` 状态点。
- 工具调用卡片使用浅琥珀色左边框或标题标识，不使用深色警告底。
- 错误状态只在局部使用红色，不把整块界面染红。
- 文本对比度优先于装饰性；小屏上不要使用低对比浅灰文字承载关键信息。

#### 明亮高级风格约束

| 项目 | 推荐 | 避免 |
|------|------|------|
| 页面背景 | `#F6F8FB`、`#F8FAFC`、暖白浅灰 | `#101522`、纯黑、深蓝大底 |
| 卡片 | 白色/雾白 + 细边框 + 极轻阴影 | 大面积深色卡片、重阴影、发光边框 |
| 状态色 | 蓝、青绿、琥珀、红色少量点缀 | 全屏高饱和渐变或霓虹色 |
| 图标 | PNG 或 Symbol + 文本兜底 | 只放图标不放文字 |
| 智能感 | 通过 Agent 状态和工具链表达 | 通过装饰性光效表达 |
| 密度 | 紧凑但留白明确 | 卡片堆叠、信息拥挤 |

466×466 圆屏上，明亮主题尤其需要注意边缘留白。页面四角天然被裁切，
卡片不要贴边；导航栏和输入栏应落在安全区域内。

#### 字体系统

| 用途 | 字体 | 大小 |
|------|------|------|
| 标题 | MiSans-Normal | 16px |
| 正文 | MiSans-Normal | 14px |
| 辅助文字 | MiSans-Normal | 12px |
| 英文/数字 | Montserrat | 对应大小 |

#### 间距系统

| 令牌 | 值 | 用途 |
|------|-----|------|
| `--spacing-xs` | 4px | 紧凑间距 |
| `--spacing-sm` | 8px | 元素内间距 |
| `--spacing-md` | 12px | 卡片内边距 |
| `--spacing-lg` | 16px | 区块间距 |
| `--spacing-xl` | 24px | 大区块间距 |

#### 圆角系统

| 令牌 | 值 | 用途 |
|------|-----|------|
| `--radius-sm` | 6px | 小按钮 |
| `--radius-md` | 8px | 工具卡片 |
| `--radius-lg` | 12px | 设备卡片 |
| `--radius-xl` | 14px | 聊天气泡 |

### 5.2 交互反馈规范

| 交互类型 | 反馈方式 | 时长 |
|---------|---------|------|
| 按钮点击 | 背景色变深 + 缩放 0.95 | 100ms |
| 卡片点击 | 背景色变深 + 阴影收缩 | 100ms |
| 开关切换 | 颜色渐变过渡 | 200ms |
| 滑块拖动 | 实时数值更新 | 即时 |
| 屏幕切换 | 左右滑动动画 | 250ms |
| 加载状态 | 旋转指示器 | 持续 |
| 成功提示 | 绿色 toast 从底部弹出 | 1500ms |
| 失败提示 | 红色 toast 从底部弹出 | 2000ms |

### 5.3 响应式适配

虽然当前目标为 466×466 圆形屏，UI 代码应使用相对布局以适配不同尺寸：

| 规则 | 实现方式 |
|------|---------|
| 宽度自适应 | `lv_pct(85)`、`lv_pct(100)` 代替固定像素 |
| 高度内容自适应 | `LV_SIZE_CONTENT` |
| 安全区域避让 | 圆形屏四角 padding：上 60px、下 76px（导航栏）、左右 50px |
| 字体缩放 | 通过 `smart_home_lvgl_style.c` 统一调整字号 |

**圆形屏安全区域计算**：

```
在 y 距离屏幕顶部 h 处，水平可视宽度为：
  visible_width = 2 × sqrt(R² - (R - h)²)
  其中 R = 233 (屏幕半径)

示例：
  h=60  → visible_width ≈ 410px
  h=100 → visible_width ≈ 430px
  h=200 → visible_width ≈ 460px (接近满宽)
```

### 5.4 错误处理

| 错误场景 | 处理方式 |
|---------|---------|
| 设备创建失败 | 弹出红色 toast："创建失败，请重试" |
| 设备控制失败 | 卡片恢复原状态 + 红色 toast |
| Agent 无响应 | 聊天界面显示超时提示气泡 |
| Agent 推理超时 | 聊天界面显示超时气泡 + "重试"按钮，参见 [5.4.1](#541-agent-超时反馈) |
| Agent 网络错误 | 聊天界面显示网络错误气泡 + "重试"按钮 |
| Agent 返回错误 | 聊天界面显示错误气泡，含错误码和简要说明 |
| LVGL 控件创建失败 | `syslog` 记录错误，不崩溃 |
| 内存不足 | `mallinfo()` 检测，拒绝创建新控件 |

#### 5.4.1 Agent 超时反馈

当 Agent 推理超时或网络错误时，在聊天信息流中插入**系统提示气泡**，与普通聊天气泡视觉区分：

```
  ┌────────────────────────┐
  │  用户: 打开客厅的灯      │  ← 用户气泡 (右对齐)
  └────────────────────────┘
  ┌────────────────────────┐
  │  ⚠ Agent 暂时无响应     │  ← 超时提示气泡 (居中, 黄色)
  │  请检查网络连接后重试    │
  │            [重试]       │  ← 重试按钮
  └────────────────────────┘
```

**超时气泡规范**：

| 属性 | 值 | 说明 |
|------|-----|------|
| 对齐 | 居中 | 与用户/Agent 气泡区分 |
| 背景色 | `#FFF6E5` 浅黄底 | 局部警告，不使用深色警告块 |
| 边框 | 左侧 3px `#FF9500` 橙色 | 与工具调用卡片一致 |
| 图标 | ⚠ `LV_SYMBOL_WARNING` | 16px，橙色 |
| 文字大小 | 12px | 辅助文字 |
| 圆角 | 8px | |
| 宽度 | 80% | |

**超时触发条件与 UI 映射**：

| Agent 错误码 | 含义 | 气泡文字 | 按钮 |
|-------------|------|---------|------|
| `AGENT_ERROR_TIMEOUT` (-6) | 整体/模型/工具超时 | "Agent 响应超时" | [重试] |
| `AGENT_ERROR_NETWORK` (-10) | 网络传输失败 | "网络连接失败，请检查网络" | [重试] |
| `AGENT_ERROR_CANCELLED` (-7) | 被取消 | "已取消" | 无（自动消失 2s） |
| `AGENT_ERROR_MODEL` (-9) | 模型返回错误 | "AI 服务暂时不可用" | [重试] |
| `AGENT_ERROR_LIMIT` (-5) | 迭代/缓冲区溢出 | "回复内容过长" | 无 |

**重试按钮行为**：

```c
static void on_retry_click(lv_event_t *e) {
    /* 重新发送上一条用户消息 */
    const char *last_input = chat_get_last_user_message();
    if (last_input) {
        /* 移除超时气泡 */
        lv_obj_del(timeout_bubble);
        /* 重新提交到 Agent */
        agent_chat_async(last_input);
    }
}
```

**超时检测实现**：通过 `agent_event_cb_t` 监听 `AGENT_EVENT_ERROR` / `AGENT_EVENT_TIMEOUT` / `AGENT_EVENT_CANCELLED` 事件。回调中通过 `lv_async_call` 投递到 LVGL 线程创建/更新超时气泡：

```c
static void demo_event_cb(const agent_event_t *event, void *user_data) {
    switch (event->type) {
    case AGENT_EVENT_TIMEOUT:
    case AGENT_EVENT_ERROR:
    case AGENT_EVENT_CANCELLED: {
        timeout_bubble_data_t *p = malloc(sizeof(*p));
        p->error_code = event->error_code;
        strncpy(p->message, event->message, sizeof(p->message) - 1);
        lv_async_call(show_timeout_bubble_cb, p);
        break;
    }
    case AGENT_EVENT_MODEL_REQUEST:
        /* 显示 "正在思考..." 指示器 */
        lv_async_call(show_thinking_indicator_cb, NULL);
        break;
    case AGENT_EVENT_MODEL_RESPONSE:
    case AGENT_EVENT_RUN_DONE:
        /* 隐藏 "正在思考..." */
        lv_async_call(hide_thinking_indicator_cb, NULL);
        break;
    default:
        break;
    }
}
```

---

### 5.5 性能要求

| 指标 | 目标值 | 测量方式 |
|------|--------|---------|
| 首次加载时间 | ≤ 3秒 | 从 `smart_home_lvgl_init()` 到首屏渲染完成 |
| 屏幕切换帧率 | ≥ 30fps | `lv_task_handler()` 周期 ≤ 33ms |
| 卡片更新延迟 | ≤ 100ms | 从 `device_state_set()` 到 UI 刷新 |
| 内存增量 | ≤ 80KB | 相比无 UI 模式的堆增量 |
| 最大设备数 | 24 (8房间×3设备) | `MAX_DEVICES` 限制 |

---

## 6. 设置页面

设置页面是底部导航的第三个入口，专门用于模型 API、网络、显示和系统配置。设备创建/编辑/删除不放在设置页，统一归属于面板。

```
┌─────────────────────────────────────────┐
│  Settings                         Ready │  ← 标题栏
├─────────────────────────────────────────┤
│                                         │
│  ╔═══════════════════════════════════╗  │
│  ║ Model API                         ║  │  ← 白色/雾白分组卡片
│  ╠═══════════════════════════════════╣  │
│  ║ Host        api.deepseek.com      ║  │
│  ║ Model       deepseek-v4-flash     ║  │
│  ║ API Key     ********              ║  │
│  ║ Timeout ms  30000                 ║  │
│  ║              [Apply]              ║  │
│  ╚═══════════════════════════════════╝  │
│                                         │
│  ╔═══════════════════════════════════╗  │
│  ║ System Status                     ║  │
│  ╠═══════════════════════════════════╣  │
│  ║ Network     Ready                 ║  │
│  ║ Runtime     openvela              ║  │
│  ║ Agent       Ready                 ║  │
│  ╚═══════════════════════════════════╝  │
│                                         │
│  ╔═══════════════════════════════════╗  │
│  ║ Display                           ║  │
│  ╠═══════════════════════════════════╣  │
│  ║ Brightness  ○───────●─── 80%     ║  │
│  ║ Theme       Light Premium         ║  │
│  ║ Tool Cards  Compact / Detailed    ║  │
│  ╚═══════════════════════════════════╝  │
│                                         │
│  ╔═══════════════════════════════════╗  │
│  ║ Maintenance                       ║  │
│  ╠═══════════════════════════════════╣  │
│  ║ Version     v2.1.0-ga3f8c21       ║  │
│  ║ Restart Agent                     ║  │
│  ║ Factory Reset                     ║  │
│  ╚═══════════════════════════════════╝  │
│                                         │
└─────────────────────────────────────────┘
```

### 6.1 设置入口

| 属性 | 值 | 说明 |
|------|-----|------|
| 入口位置 | 底部导航第三项 | Settings 图标或文字 |
| 图标尺寸 | 24×24px | |
| 图标颜色 | `#8A96A8`，与导航非激活状态一致 | |
| 进入动画 | 从右滑入, 250ms | `LV_SCR_LOAD_ANIM_MOVE_LEFT` |
| 返回方式 | 点击底部导航其他 tab / 物理返回键 | 返回到目标屏幕 |

### 6.2 AI 服务设置

调整 LLM 后端配置，修改后**立即生效**（下一次 Agent 调用使用新配置）：

| 设置项 | 控件类型 | 说明 |
|--------|---------|------|
| Host | 文本输入 `lv_textarea` | 如 `api.deepseek.com` |
| Model | 文本输入 `lv_textarea` | 如 `deepseek-v4-flash` |
| API Key | 文本输入 `lv_textarea` (密码模式) | 输入时显示 `*`，提供 [👁 显示] 切换 |
| Timeout ms | 数字文本输入 | 默认 30000，影响后续请求 timeout |
| Apply | 文本按钮 `lv_btn` | 应用到当前运行 session |

**API Key 安全规则**：

| 规则 | 说明 |
|------|------|
| 星号遮盖 | 输入框默认显示 `****`，仅显示最后 4 位 |
| 不可读取 | 设置页面不可回显完整 key，修改时必须完整重新输入 |
| 存储位置 | 当前阶段只在内存生效；持久化到 `/data` 作为后续能力 |
| 内存保护 | 不在日志、UI 文本、event trace 中打印 key 值 |

**模型后端预设**：

```c
typedef struct {
    const char *name;      /* 显示名 */
    const char *host;      /* API host */
    const char *path;      /* API path */
    const char *port;      /* 端口 */
} llm_backend_preset_t;

static const llm_backend_preset_t s_presets[] = {
    { "DeepSeek",      "api.deepseek.com",     "/v1/chat/completions", "443" },
    { "Kimi",          "api.moonshot.cn",      "/v1/chat/completions", "443" },
    { "通义千问",       "dashscope.aliyuncs.com", "/compatible-mode/v1/chat/completions", "443" },
    { "自定义",         "",                      "",                      ""   },
};
```

选择预设后自动填充 host/path/port，API key 和 model 仍需用户手动输入。

### 6.3 网络设置

| 设置项 | 控件类型 | 说明 |
|--------|---------|------|
| Wi-Fi 名称 | 下拉选择 `lv_dropdown` | 扫描周边 Wi-Fi SSID 列表 (需 Wi-Fi 驱动支持) |
| Wi-Fi 密码 | 文本输入 `lv_textarea` (密码模式) | 连接新网络时输入 |
| IP 地址 | 只读文本 | 显示当前 IP，如 "192.168.1.100" |
| 网络状态 | 只读文本 + 颜色指示 | ● 绿=已连接 / ○ 灰=未连接 / ◌ 黄=连接中 |

网络状态通过 `agent_event_cb_t` 监听网络变更事件来更新显示。Wi-Fi 扫描需要在 LVGL 之外的网络管理线程中完成，结果通过 `lv_async_call` 回传。

### 6.4 显示设置

| 设置项 | 控件类型 | 默认值 | 说明 |
|--------|---------|--------|------|
| 屏幕亮度 | 滑块 `lv_slider` | 80% | 范围 10-100%，步进 5% |
| 主题 | 只读文本 / 后续分段按钮 | Light Premium | 默认保持明亮高级；Dark 仅作为后续完整主题能力 |
| 工具卡片模式 | 分段按钮 | 简洁 | 简洁: 仅显示工具名 + 状态图标 / 详细: 显示完整参数 + JSON |

**工具卡片模式**的具体行为差异：

| 显示元素 | 简洁模式 | 详细模式 |
|---------|---------|---------|
| 工具名 + 状态图标 | ✅ | ✅ |
| 参数列表 (device/room/brightness) | ❌ | ✅ |
| 结果 JSON | ❌ | ✅ (可折叠) |
| 展开/折叠按钮 | ❌ | ✅ |
| 重试按钮 | ✅ (仅失败时) | ✅ (仅失败时) |

### 6.5 系统信息与维护

| 设置项 | 控件类型 | 说明 |
|--------|---------|------|
| 固件版本 | 只读文本 | 从 `AGENT_BUILD_VERSION` 宏读取 |
| cAGENT 版本 | 只读文本 | 从 `agent.h` 的版本宏读取 |
| 运行统计 | 只读文本 | 累计运行次数、模型调用次数、工具调用次数 |
| 重启 Agent | 按钮 (橙色) | 调用 `agent_reset()` → 清除 sessions + 重置 stats |
| 恢复出厂设置 | 按钮 (红色) | 确认对话框 → 清除所有配置 + 恢复内置默认值 |

**运行统计**通过 `agent_get_stats()` 获取并每秒刷新：

```c
static void refresh_stats_timer_cb(lv_timer_t *timer) {
    agent_stats_t stats;
    agent_get_stats(g_agent, &stats);
    lv_label_set_text_fmt(stats_label,
        "运行: %u 次 | 模型调用: %u | 工具调用: %u",
        stats.runs, stats.model_calls, stats.tool_calls);
}
```

**恢复出厂设置**的确认对话框：

```
┌──────────────────────────┐
│                          │
│   ⚠ 恢复出厂设置          │
│                          │
│  这将清除所有配置和数据：   │
│  - API Key 和模型配置      │
│  - Wi-Fi 密码             │
│  - 自定义设备              │
│  - 对话历史               │
│                          │
│  此操作不可撤销！          │
│                          │
│  ┌────────┐ ┌──────────┐│
│  │  取消   │ │ 确认恢复  ││
│  └────────┘ └──────────┘│
│                          │
└──────────────────────────┘
```

恢复流程：
1. 弹出确认对话框
2. 用户点击「确认恢复」
3. 删除 `/data/agent/config/` 下所有 `.conf` 文件
4. 调用 `agent_reset()` 清除 sessions 和 stats
5. 写入内置默认配置文件
6. 重启 Agent 进程（或重新初始化 agent 实例）
7. 显示引导页 / 初始设置向导

### 6.6 初始设置向导

当设备首次启动或恢复出厂设置后，显示**初始设置向导**（全屏覆盖，不可跳过）：

```
┌─────────────────────────────────────────┐
│                                         │
│           🤖 欢迎使用智能家居            │
│                                         │
│  请配置 AI 服务以开始使用                │
│                                         │
│  1. 选择模型后端                         │
│     ┌──────────────────────────┐       │
│     │ DeepSeek              ▼  │       │
│     └──────────────────────────┘       │
│                                         │
│  2. 输入 API Key                        │
│     ┌──────────────────────────┐       │
│     │ sk-********************  │       │
│     └──────────────────────────┘       │
│                                         │
│  3. 连接 Wi-Fi (可选)                   │
│     ┌──────────────────────────┐       │
│     │ 选择网络...             ▼│       │
│     └──────────────────────────┘       │
│                                         │
│  ┌──────────────────────────────┐       │
│  │          开始使用             │       │  ← 验证通过后启用
│  └──────────────────────────────┘       │
│                                         │
└─────────────────────────────────────────┘
```

验证规则：
- API Key 非空（最小 8 字符）
- 模型名称非空
- `[开始使用]` 按钮在所有必填项填写完毕后变为蓝色可点击状态，否则为灰色禁用态

---

## 7. 交付物

### 7.1 UI 设计稿

本文档即作为 UI 设计规范，包含所有状态和交互效果的文字描述。视觉稿以 ASCII 线框图形式嵌入各章节。

### 7.2 组件规范文档

| 组件 | 规范位置 |
|------|---------|
| 底部导航栏 | [1. 导航设计规范](#1-导航设计规范) |
| 设备卡片 | [2. 设备卡片组件规范](#2-设备卡片组件规范) |
| 设备控制弹窗 | [2.4 设备控制弹窗](#24-设备控制弹窗) |
| 房间过滤选择器 | [2.7 房间过滤](#27-房间过滤) |
| 工具调用卡片 | [3. 工具调用卡片设计规范](#3-工具调用卡片设计规范) |
| PTT 语音输入 | [3.7 语音输入-ptt-交互](#37-语音输入-ptt-交互) |
| 超时/错误提示气泡 | [5.4.1 Agent 超时反馈](#541-agent-超时反馈) |
| 设备管理列表 | [4. 面板设备管理功能规范](#4-面板设备管理功能规范) |
| 设备创建表单 | [4.2 设备创建流程](#42-设备创建流程) |
| 设置页面 | [6. 设置页面](#6-设置页面) |
| 初始设置向导 | [6.6 初始设置向导](#66-初始设置向导) |
| Toast 提示 | [5.2 交互反馈规范](#52-交互反馈规范) |
| 空状态 | [4.5 空状态展示](#45-空状态展示) |

### 7.3 交互流程图

#### 主流程

```
启动
 │
 ▼
┌──────────┐    点击[面板]    ┌──────────┐
│  设备面板  │ ◄────────────── │  设备面板  │
│  (主屏)   │ ──────────────► │  (主屏)   │
└──────────┘    点击[对话]    └──────────┘
     │                           │
     │ 点击卡片                   │ PTT/输入
     ▼                           ▼
┌──────────┐              ┌──────────┐
│  设备控制  │              │  聊天界面  │
│  弹窗     │              │  +工具卡片 │
└──────────┘              └──────────┘
     │                           │
     │ 确认/取消                  │ Agent回复
     ▼                           ▼
  返回面板                   显示结果
                                │
     点击[设置]                  │
     ────────────────────────────│
     ▼                           │
┌──────────┐                     │
│  设置页面  │                     │
│  模型/API │                     │
└──────────┘                     │
```

设备新建/编辑/删除从面板标题栏或设备卡片进入，完成后返回面板。

#### 设备控制交互流程

```
点击设备卡片
    │
    ▼
弹出控制界面
    │
    ├─ 拖动滑块 → 实时预览参数值
    ├─ 点击开关 → 切换 ON/OFF 预览
    │
    ├─ 点击「确认」
    │   ├─ 弹窗显示加载态
    │   ├─ 调用 device_state_set()
    │   ├─ 成功 → 关闭弹窗 + 卡片刷新 + 绿色toast
    │   └─ 失败 → 关闭弹窗 + 卡片恢复 + 红色toast
    │
    └─ 点击「取消」/「返回」
        └─ 关闭弹窗，状态不变
```

#### Agent 工具调用交互流程

```
用户发送消息 (PTT/文本)
    │
    ▼
聊天界面显示用户气泡
    │
    ▼
Agent 推理中... (显示"正在思考")
    │
    ▼
Agent 调用工具
    ├─ 插入工具调用卡片 (加载中)
    ├─ 工具执行完成
    ├─ 更新卡片 (成功/失败)
    └─ 若为设备控制工具 → 同步刷新面板卡片
    │
    ▼
Agent 回复
    └─ 插入 Agent 回复气泡
```

### 7.4 响应式设计规范

| 屏幕尺寸 | 布局调整 |
|---------|---------|
| 466×466 (当前) | 2列卡片网格，标准间距 |
| 320×320 (小屏) | 2列卡片网格，缩小间距和字号 |
| 800×480 (横屏) | 3-4列卡片网格，侧边导航 |

**实现原则**：
- 所有尺寸使用 `lv_pct()` 或基于屏幕宽度的计算
- 字体大小通过宏定义，可全局调整
- 卡片列数根据屏幕宽度动态计算

### 7.5 切图资源包

LVGL UI 可使用 PNG 图标提升产品质感，但图标不应成为理解界面的唯一途径。
所有关键操作必须保留文字标签或文字兜底，避免 PNG 资源缺失时界面不可用。

| 资源类型 | 方案 | 说明 |
|---------|------|------|
| 导航图标 | PNG + 文本 | 图标来自 `res/icons/icon_nav_*.png`，文字保留 `Panel` / `Chat` / `Settings` |
| 设备图标 | PNG + 设备名 | 图标来自 `icon_device_*.png`，文字显示设备名称和状态 |
| 状态图标 | 文字 | 当前实现使用 `ON`、`OFF`、`OK`、`Failed`、`Running` |
| 工具图标 | PNG 或文字 | 工具卡片标题始终显示 `Tool Call` |
| 箭头图标 | LVGL Symbol | 需要展开/返回时优先使用 LVGL Symbol |

---

## 8. 附录：技术约束与参考

### 8.1 平台约束

| 约束 | 值 | 影响 |
|------|-----|------|
| 屏幕分辨率 | 466×466 | 圆形屏，四角裁切 |
| 色深 | RGB565 (16bit) | 最多 65536 色，渐变可能有条纹 |
| 帧缓冲 | 单缓冲 + 部分刷新 | 避免全屏重绘 |
| DPI | 130 | 图标和文字需足够大 |
| CJK 字体 | MiSans-Normal (FreeType) | 需运行时创建，必须在 LVGL 线程 |
| 堆内存 | ~2MB 可用 | 控件数量需限制 |

字体加载路径由 `CONFIG_SMART_HOME_DEMO_DATA_ROOT` 控制，默认：

```text
/data/res/fonts/MiSans-Normal.ttf
```

代码通过 `smart_home_lvgl_style_init()` 创建 12/14/16/20 四档字体。
若 FreeType 或字体文件不可用，回退到 LVGL Montserrat，英文仍可显示，但中文不完整。

PNG 图标从 `CONFIG_SMART_HOME_DEMO_UI_LVGL_ICONS` 指定目录加载，默认
`/data/res/icons`。源码 PNG 位于 `packages/demos/smart_home/res/icons/`，
需要通过 ROMFS 或 data partition 部署到目标路径。LVGL 配置需启用
`LV_USE_LIBPNG` 或 `LV_USE_LODEPNG`。

### 8.2 LVGL 线程安全规则

1. **所有 LVGL 控件操作必须在 LVGL 线程中执行**
2. 外部线程（Agent loop、工具执行线程）通过 `lv_async_call()` 调度 UI 更新
3. `lv_async_call` 的回调中可安全创建/修改/删除控件
4. 回调中分配的 payload 必须在回调结束时 `free`
5. **`agent_event_cb_t` 回调在 Agent 推理线程中同步执行**，不可直接操作 LVGL 控件。需更新 UI 时，必须在回调中 `malloc` 数据并通过 `lv_async_call` 投递到 LVGL 线程
6. 音频采集线程、网络管理线程等同样适用规则 2

```c
/* 正确：从 Agent 线程更新 UI */
typedef struct {
    char device[32];
    int state;
} device_update_t;

static void device_update_async_cb(void* data) {
    device_update_t* p = (device_update_t*)data;
    /* 安全地更新 LVGL 控件 */
    update_device_card(p->device, p->state);
    free(p);  /* 必须释放 */
}

void lvgl_ui_device_update(const char* device, int state) {
    device_update_t* p = malloc(sizeof(device_update_t));
    if (!p) return;
    strncpy(p->device, device, sizeof(p->device) - 1);
    p->state = state;
    lv_async_call(device_update_async_cb, p);
}
```

### 8.3 当前代码结构

| 文件 | 说明 |
|------|------|
| `src/ui/lvgl/smart_home_lvgl.c` | LVGL 初始化、屏幕创建、run loop |
| `src/ui/lvgl/smart_home_lvgl_style.c` | 颜色、卡片样式、可选字体资源 |
| `src/ui/lvgl/smart_home_lvgl_nav.c` | 底部导航 |
| `src/ui/lvgl/smart_home_lvgl_panel.c` | 设备面板、卡片刷新、控制弹窗 |
| `src/ui/lvgl/smart_home_lvgl_chat.c` | 聊天气泡、工具调用卡片、文本输入栏 |
| `src/ui/lvgl/smart_home_lvgl_settings.c` | Host、Model、API Key、Timeout 运行时配置表单 |
| `src/ui/lvgl/smart_home_lvgl_agent.c` | Agent worker、事件回调、`lv_async_call` 回写 |
| `src/ui/lvgl/smart_home_lvgl_internal.h` | LVGL 内部尺寸、tab、keyboard helper、模块函数声明 |

### 8.4 后续可新增的接口

| 接口 | 文件 | 说明 |
|------|------|------|
| `smart_home_lvgl_panel_rebuild_devices()` | `smart_home_lvgl_panel.c` | 支持动态设备增删后的卡片重建 |
| `smart_home_lvgl_tool_card_update()` | `smart_home_lvgl_chat.c` | 将当前“追加成功/失败卡片”升级为原卡片状态更新 |
| `smart_home_settings_load/save()` | `src/config/` | 保存 host/model/API key 等配置 |
| `smart_home_device_remove(room, device)` | `src/device/` | 删除设备 |
| `smart_home_device_set_param(room, device, key, val)` | `src/device/` | 设置设备参数 |

### 8.5 Kconfig 配置

启用 LVGL UI 需要在 menuconfig 中设置：

```
Application Configuration → Packages → [*] Vela AI Agent
    → [*] Smart home demo
        → Smart home UI backend
            → [*] LVGL UI  (CONFIG_SMART_HOME_DEMO_UI_LVGL=y)
```

依赖关系：
- `CONFIG_GRAPHICS_LVGL=y` (已启用)
- `CONFIG_LV_USE_FREETYPE=y`，用于可选 MiSans 字体加载
- `CONFIG_SMART_HOME_DEMO_DATA_ROOT="/data"`，用于定位字体资源
- `CONFIG_LV_USE_LIBPNG=y` 或 `CONFIG_LV_USE_LODEPNG=y`，用于 PNG 图标
- `CONFIG_SMART_HOME_DEMO_UI_LVGL_ICONS="/data/res/icons"`，用于定位 PNG 图标
